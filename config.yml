# Galaxy Profile README Configuration
# Copy this file to config.yml and customize it to make it your own!
#
# Usage:
#   cp config.example.yml config.yml
#   # Edit config.yml with your details
#   python -m generator.main
#
# Demo mode (no API calls, uses this file directly):
#   python -m generator.main --demo

# Your GitHub username (required)
username: lucasfsduarte

# Profile information displayed in the galaxy header
profile:
  name: "Lucas Duarte"
  tagline: "Tech Lead & Full Stack Developer"
  company: "Hubs Contabilidade"
  location: "Cascavel, PR"
  bio: |
    I understand problems and solve them in the best way possible.
    Specialty coffee enthusiast and fan of high-performance sports (swimming, running and cycling).
    Most of my projects are private code.
  philosophy: '"Go one more."'

# Social links displayed at the bottom of the profile
social:
  email: "lucasfsduarte@gmail.com"
  linkedin: "lucasfsduarte"
  website: "https://lucasfsduarte.github.io"

# Galaxy arms represent your core focus areas
# Each arm spirals out from the center with its own color and tech items
# Available colors: "synapse_cyan", "dendrite_violet", "axon_amber"
galaxy_arms:
  - name: "Frontend"
    color: "axon_amber"
    items:
      - "TypeScript"
      - "React"
      - "React Native"
      - "Expo"
      - "Next"
      - "Figma"
      - "Design Systems"
  - name: "Backend"
    color: "dendrite_violet"
    items:
      - "Node.js"
      - "GraphQL"
      - "Python"
      - "Elixir"
      - "OLTP DBs"
      - "Phoenix"
      - "Nest"
  - name: "Others"
    color: "synapse_cyan"
    items:
      - "Docker"
      - "GitHub Actions"
      - "AWS"
      - "CI/CD"
      - "OLAP DBs"
      - "Elastic Search"
      - "IA Integrations"
      - "IA models"

# Featured projects shown as constellation stars
# Each project needs:
#   repo: "owner/repo-name"   — GitHub repository path
#   arm: 0-2                  — Which galaxy arm to associate with (index)
#   description: "..."        — Short description shown on the card
# projects:
#   - repo: "galaxy-dev/nebula-ui"
#     arm: 0
#     description: "A component library with cosmic design tokens and dark-first theming."
#   - repo: "galaxy-dev/stargate-api"
#     arm: 1
#     description: "High-performance API gateway with built-in observability and caching."

# Theme color overrides (optional — defaults to deep space palette)
# All values must be valid hex color codes (#RRGGBB)
# theme:
#   void: "#080c14"              # Darkest background
#   nebula: "#0f1623"            # Card backgrounds
#   star_dust: "#1a2332"         # Borders, dividers
#   synapse_cyan: "#00d4ff"      # Primary accent (arm color 1)
#   dendrite_violet: "#a78bfa"   # Secondary accent (arm color 2)
#   axon_amber: "#ffb020"        # Tertiary accent (arm color 3)
#   text_bright: "#f1f5f9"       # Primary text
#   text_dim: "#94a3b8"          # Secondary text
#   text_faint: "#64748b"        # Muted text, labels
theme:
  void: "#081408" # Deep forest black
  nebula: "#08140c" # Elevated dark green surface
  star_dust: "#21321a" # Borders / separators (mossy green)

  synapse_cyan: "#6ad400" # Green-leaning teal
  dendrite_violet: "#488c37" # Muted violet with slight cool green harmony
  axon_amber: "#a2c42f" # Earthy amber (less orange, more natural)

  text_bright: "#eef2ef" # Soft neutral white
  text_dim: "#a7b3ac" # Sage grey
  text_faint: "#6f8078" # Muted moss grey

# Stats configuration — which metrics to show on the telemetry card
stats:
  metrics:
    - "commits"
    - "stars"
    - "prs"
    - "issues"
    - "repos"

# Language display settings
languages:
  exclude: # Languages to hide from the telemetry chart
    - "HTML"
    - "CSS"
    - "Shell"
    - "Makefile"
  max_display: 8 # Maximum number of languages to show
